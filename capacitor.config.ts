import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bakikorkmaz.bakininmutfagi',
  appName: 'bakinin-mutfagi',
  webDir: 'build'
};

export default config;
